A set of scripts to use DESC cosmology pipelines (CCL, Firecrown, TJPCov, etc) for simple MCP applications (data vector generation, MCMC, etc). 

At NERSC, run the following command to load the environment needed to use MCP softwares. 
< source $CFS/lsst/groups/MCP/setup-cosmology.sh > 
